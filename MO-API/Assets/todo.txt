実装予定
・Azure/Amazon Redis/Docker/Swaggerのいずれかで通信サーバーを走らせる。 -> Azureで実装する
・スコアを記録するDBサーバーの用意 -> SQLで作る。 クライアント=>サーバー=>SQLとなる予定
・スコアのソート -> 二分探索。 鯖側実装
・物理を実装 -> ほとんど完成 落ちる概念を実装する
・ステージはα版ではProBuilderで適当に作る -> すぐできる
・インベントリ作り直し -> ローカライズするため
・プレイヤーの移動関係を組む -> 進捗ゼロ
・α版の完成を急ぐ

・時間があれば保存アセットのArrayPool<byte>対応。